document.addEventListener('DOMContentLoaded', () => {
  const body = document.querySelector('body')
  console.log(
    '🚀 ~ file: jd-modal.ts:3 ~ document.addEventListener ~ body:',
    body
  )
})
